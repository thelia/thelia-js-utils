{
  "name": "@thelia/blocks-plugin-manager",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "license": "MIT",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --env.NODE_ENV production",
    "dev": "tsup src/index.ts --format esm --watch --dts",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@thelia/blocks-components": "*",
    "react": "*"
  },
  "devDependencies": {
    "tsup": "^5.10.1",
    "typescript": "^4.5.3",
    "nanoid": "^3.1.20"
  },
  "publishConfig": {
    "access": "public"
  }
}
